{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Pull from Git if no uncommitted changes",
            "type": "shell",
            "command": "git diff-index --quiet HEAD -- && git pull || echo 'Uncommitted changes detected, skipping pull'",
            "problemMatcher": [],
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}